# Web服务端口
server:
  port: 8080

# 连接数据库的配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ssm_task?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Chongqing
    username: root
    password: root

# 日志的显示级别
logging:
  level:
    cn.maplerabbit.rlg: trace